[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "open-deepthink"
dynamic = ["version"]
description = "Qualitative Neural Networks, Qualitative Diffusion (QDAD), and evolutionary knowledge distillation — Python library + optional web UI"
readme = "README.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }
authors = [
    { name = "iblameandrew" }
]
keywords = [
    "ai", "multi-agent", "qnn", "qdad", "langgraph", "llm",
    "openrouter", "llamacpp", "knowledge-distillation",
    "qualitative-diffusion", "mirror-descent",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Framework :: FastAPI",
]
# Core = algorithms library (QNN / QDAD / distillation). Web UI is optional.
dependencies = [
    "python-dotenv",
    "aiohttp",
    "langchain-core",
    "langchain-community",
    "langchain-openai",
    "langchain-text-splitters",
    "langgraph",
    "grandalf",
    "faiss-cpu",
    "scikit-learn",
    "numpy",
    "pymupdf>=1.24.0",
    "names",
    "openai",
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
]

[project.optional-dependencies]
web = [
    "fastapi",
    "uvicorn[standard]",
    "sse-starlette",
    "python-multipart",
]
dev = [
    "open-deepthink[web]",
    "ruff>=0.6.0",
    "mypy>=1.8.0",
    "types-requests",
    "httpx",
]
skills = []
ui = [
    "open-deepthink[web]",
]
all = [
    "open-deepthink[dev,web]",
]

[project.urls]
"Homepage" = "https://github.com/iblameandrew/open-deepthink"
"Repository" = "https://github.com/iblameandrew/open-deepthink"
"Issues" = "https://github.com/iblameandrew/open-deepthink/issues"
"Changelog" = "https://github.com/iblameandrew/open-deepthink/blob/main/RELEASE_NOTES.md"

[project.scripts]
open-deepthink = "deepthink.cli:main"
deepthink = "deepthink.cli:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["deepthink*"]
exclude = ["tests*", "dist*", "terminals*", ".skill-runs*", ".grok*"]

[tool.setuptools.dynamic]
version = { attr = "deepthink.__version__" }

[tool.setuptools.package-data]
deepthink = ["py.typed"]

# ---------------------------------------------------------------------------
# Tooling
# ---------------------------------------------------------------------------

[tool.ruff]
target-version = "py310"
line-length = 100
src = ["deepthink", "tests", "app.py"]
extend-exclude = ["dist", "distillation_output", ".skill-runs", "terminals"]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "F",   # pyflakes
    "I",   # isort
    "W",   # pycodestyle warnings
    "UP",  # pyupgrade
]
ignore = [
    "E501",  # line length — handled softly; many prompts are long
    "E402",  # module level import not at top — app.py path setup
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["F401", "F841", "E402"]
"app.py" = ["E402", "F401"]

[tool.mypy]
python_version = "3.10"
ignore_missing_imports = true
warn_unused_ignores = true
warn_return_any = false
check_untyped_defs = false
disallow_untyped_defs = false
follow_imports = "skip"
# Gradual typing — library surface
files = [
    "deepthink/config.py",
    "deepthink/cli.py",
    "deepthink/__main__.py",
    "deepthink/providers.py",
    "deepthink/api.py",
]

[tool.pytest.ini_options]
# Legacy suite uses custom chk() scripts; CI invokes tests/run_all.py
testpaths = ["tests"]
python_files = ["phase*.py"]
