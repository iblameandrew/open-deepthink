# =============================================================================
# open-deepthink dependencies
# Preferred install:  pip install -e ".[dev]"
# This file mirrors core runtime deps for simple `pip install -r` workflows.
# Providers: OpenRouter (cloud) and LlamaCpp Server (local).
# =============================================================================

# Web / server
fastapi
uvicorn[standard]
python-dotenv
sse-starlette
python-multipart
aiohttp

# LangChain ecosystem
langchain-core
langchain-community  # FAISS vectorstore
langchain-openai
langchain-text-splitters
langgraph
grandalf

# Vector store + ML
faiss-cpu
scikit-learn
numpy

# Document processing + config
pymupdf>=1.24.0
names
openai
pydantic>=2.0
pydantic-settings>=2.0
