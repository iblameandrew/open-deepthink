# =============================================================================
# open-deepthink dependencies
# Preferred:
#   pip install -e .              # algorithms library (QNN / QDAD / distillation)
#   pip install -e ".[web]"       # + FastAPI UI
#   pip install -e ".[dev]"       # + web + ruff/mypy
# This file = library + web (full app) for simple `pip install -r` workflows.
# Providers: OpenRouter (cloud) and LlamaCpp Server (local).
# =============================================================================

# Library core
python-dotenv
aiohttp
langchain-core
langchain-community  # FAISS vectorstore
langchain-openai
langchain-text-splitters
langgraph
grandalf
faiss-cpu
scikit-learn
numpy
pymupdf>=1.24.0
names
openai
pydantic>=2.0
pydantic-settings>=2.0

# Web UI (optional if you only use the library API)
fastapi
uvicorn[standard]
sse-starlette
python-multipart
